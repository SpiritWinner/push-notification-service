# Push Notification Server

Сервер для отправки push-уведомлений через Expo для React Native приложений.

## 📋 Требования

- Node.js 18+
- PostgreSQL 12+
- npm или yarn

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd push-notification-server
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка окружения

Создайте файл `.env` в корне проекта на основе `.env.example`:

```env
# Обязательные настройки
DATABASE_URL="postgresql://username:password@localhost:5432/database_name"
PORT=3000
NODE_ENV=development
```

### 4. Настройка базы данных

Локально через Prisma:
```bash
# Сгенерировать Prisma клиент
npm run prisma:generate

# Создать и применить миграции
npm run prisma:migrate
```

Через Docker:
```bash
# Собрать Docker образы
npm run docker:build

# Поднять контейнеры (Postgres + сервер)
npm run docker:up:detached

# Сгенерировать Prisma клиент внутри контейнера
npm run docker:prisma:push

# Применить миграции на production базе через контейнер
npm run docker:prisma:deploy

# Смотреть логи сервера
npm run docker:logs
```
### 5. Запуск сервера

#### Режим разработки (с hot-reload):
```bash
npm run dev
```

#### Продакшен сборка:
```bash
npm run build
npm start
```

#### Через Docker:
```bash
npm run docker:up:detached
```

#### Остановка Docker контейнеров:
```bash
npm run docker:down
```

Сервер будет доступен по адресу: `http://localhost:3000`

## 📚 API Документация

После запуска сервера документация доступна по адресам:

- 📖 Swagger UI: `http://localhost:3000/api-docs`
- 📄 OpenAPI спецификация: `http://localhost:3000/api-docs.json`
- 🩺 Health check: `http://localhost:3000/health`

## 🔐 Аутентификация

API использует простую Bearer токен аутентификацию. В качестве токена используется `user_id`:

```http
Authorization: Bearer your_user_id_here
```

Пример:
```
Authorization: Bearer user123
```

## 📱 Основные Endpoints

### Устройства

| Метод | Endpoint | Описание | Аутентификация |
|-------|----------|----------|----------------|
| POST | `/api/register` | Регистрация устройства | Да |
| POST | `/api/verify-token` | Проверка токена устройства | Да |
| GET | `/api/token-info` | Информация о токене | Да |
| DELETE | `/api/unregister` | Удаление устройства | Да |
| GET | `/api/me` | Информация о текущем пользователе | Да |
| GET | `/api/users` | Список всех пользователей | Нет |

### Уведомления

| Метод | Endpoint | Описание | Аутентификация |
|-------|----------|----------|----------------|
| POST | `/api/send` | Отправить уведомление себе | Да |
| POST | `/api/test-token` | Тестовое уведомление | Да |
| POST | `/api/broadcast` | Рассылка всем (только admin) | Да (admin) |
| GET | `/api/history` | История уведомлений (только admin) | Да (admin) |

### Health & Info

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/` | Информация о сервере |
| GET | `/health` | Health check для мониторинга |

## 📝 Примеры запросов

### Регистрация устройства

```bash
curl -X POST http://localhost:3000/api/register \
  -H "Authorization: Bearer user123" \
  -H "Content-Type: application/json" \
  -d '{
    "expoPushToken": "ExponentPushToken[xxxxxxxxxxxxxxxxxxxxxx]",
    "platform": "ios",
    "appVersion": "1.0.0",
    "deviceName": "iPhone 12",
    "deviceModel": "iPhone13,2"
  }'
```

### Отправка уведомления себе

```bash
curl -X POST http://localhost:3000/api/send \
  -H "Authorization: Bearer user123" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Новое сообщение",
    "body": "У вас новое сообщение в чате",
    "data": {
      "type": "message",
      "chatId": "123"
    }
  }'
```

### Тестовое уведомление

```bash
curl -X POST http://localhost:3000/api/test-token \
  -H "Authorization: Bearer user123"
```

## 🗄️ База данных

Проект использует **PostgreSQL** с **Prisma ORM**. Структура базы данных автоматически создается при запуске миграций.

### Основные таблицы:

1. **devices** - зарегистрированные устройства
2. **notifications** - история отправленных уведомлений

Для просмотра и управления данными используйте Prisma Studio:
```bash
npm run prisma:studio
```

## 📦 Структура проекта

```
push-notification-server/
├── src/
│   ├── controllers/     # Контроллеры API
│   ├── services/        # Бизнес-логика
│   ├── routes/          # Маршруты API
│   ├── middleware/      # Промежуточное ПО
│   ├── prisma/         # Prisma клиент
│   ├── config/         # Конфигурации
│   ├── types/          # TypeScript типы
│   ├── utils/          # Вспомогательные функции
│   ├── app.ts          # Express приложение
│   └── server.ts       # Точка входа
├── prisma/
│   └── schema.prisma   # Схема базы данных
├── dist/               # Скомпилированные файлы
├── .env               # Переменные окружения
├── package.json       # Зависимости и скрипты
├── tsconfig.json      # Конфигурация TypeScript
└── README.md          # Документация
```

## 🛠️ Доступные команды

```bash
# Разработка
npm run dev          # Запуск в режиме разработки
npm run build        # Сборка проекта
npm run build:watch  # Сборка с отслеживанием изменений

# База данных
npm run prisma:generate  # Генерация Prisma клиента
npm run prisma:migrate   # Применение миграций
npm run prisma:studio    # Открыть Prisma Studio
npm run prisma:deploy    # Деплой миграций (продакшен)

# Запуск
npm start           # Запуск продакшен версии
```

## 🔧 Технические детали

### Используемые технологии:
- **Node.js** с **TypeScript**
- **Express.js** - веб-фреймворк
- **Prisma** - ORM для PostgreSQL
- **Expo Server SDK** - отправка push-уведомлений
- **Swagger/OpenAPI** - документация API

### Особенности:
- Полная TypeScript типизация
- Автоматическая генерация документации API
- Поддержка Expo push токенов
- История отправленных уведомлений
- Админ-панель для рассылок
- Health check для мониторинга
