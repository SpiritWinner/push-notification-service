# Push Notification Service - Monorepo

Монорепозиторий для системы push-уведомлений с бэкендом на Node.js и админ-панелью на React.

## 📋 Требования

- Node.js >= 18.0.0
- pnpm >= 8.0.0
- Docker и Docker Compose (опционально)
- PostgreSQL 12+ (для бэкенда)

## 🏗️ Структура проекта

```
push-notification-service/
├── apps/
│   ├── backend/          # Бэкенд сервис (Node.js + Express + TypeScript)
│   └── frontend/         # Админ-панель (React + Vite + TypeScript)
├── packages/
│   └── shared/           # Общие утилиты и типы
├── docker/
│   └── docker-compose.yml # Конфигурация Docker
├── package.json          # Корневой package.json
└── README.md            # Эта документация
```

## 🚀 Быстрый старт

### 1. Клонирование и установка

```bash
# Клонирование репозитория
git clone <repository-url>
cd push-notification-service

# Установка зависимостей
pnpm install
```

### 2. Настройка окружения

#### Бэкенд
Создайте файл `apps/backend/.env` на основе `apps/backend/.env.example`:
```env
DATABASE_URL="postgresql://username:password@localhost:5432/notification_db"
PORT=3000
NODE_ENV=development
```

#### Фронтенд
Создайте файл `apps/frontend/.env` на основе `apps/frontend/.env.example`:
```env
VITE_API_URL=http://localhost:3000
```

### 3. Запуск через Docker (рекомендуется)

```bash
# Сборка и запуск всех сервисов
pnpm run docker:up:detached

# Проверка статуса контейнеров
pnpm run docker:ps

# Просмотр логов
pnpm run docker:logs
```

### 4. Запуск в режиме разработки (без Docker)

```bash
# Сборка shared пакета (требуется перед запуском приложений)
pnpm run shared:build

# Запуск всех сервисов через Turbo (бэкенд + фронтенд)
pnpm run dev

# Или запуск отдельных сервисов
pnpm run backend:dev    # только бэкенд (localhost:3000)
pnpm run frontend:dev   # только фронтенд (localhost:5173)
```

## 📦 Доступные команды

### Общие команды
```bash
pnpm run dev              # Запуск всех сервисов в режиме разработки
pnpm run build           # Сборка всех сервисов
pnpm run start           # Запуск всех сервисов в продакшен режиме
pnpm run test            # Запуск тестов во всех сервисах
pnpm run lint            # Проверка кода линтером
pnpm run format          # Форматирование кода
pnpm run clean           # Очистка всех сборок и зависимостей
```

### Команды для отдельных сервисов
```bash
# Бэкенд
pnpm run backend:dev     # Запуск бэкенда в режиме разработки

# Фронтенд
pnpm run frontend:dev    # Запуск фронтенда в режиме разработки

# Shared пакет
pnpm run shared:build    # Сборка shared пакета
```

### Docker команды
```bash
pnpm run docker:up                    # Запуск контейнеров
pnpm run docker:up:detached          # Запуск контейнеров в фоновом режиме
pnpm run docker:down                 # Остановка контейнеров
pnpm run docker:build                # Сборка Docker образов
pnpm run docker:restart              # Перезапуск контейнеров

# Логи
pnpm run docker:logs                 # Логи всех контейнеров
pnpm run docker:logs:backend        # Логи бэкенда
pnpm run docker:logs:frontend       # Логи фронтенда
pnpm run docker:logs:db             # Логи базы данных

# Действия внутри контейнеров
pnpm run docker:exec:backend        # Вход в контейнер бэкенда
pnpm run docker:exec:frontend       # Вход в контейнер фронтенда

# Информация
pnpm run docker:ps                  # Статус контейнеров
```

## 🌐 Доступ к сервисам

После запуска:

### Бэкенд (API сервер)
- **URL**: http://localhost:3000
- **API документация**: http://localhost:3000/api-docs
- **Health check**: http://localhost:3000/health
- **OpenAPI спецификация**: http://localhost:3000/api-docs.json

### Фронтенд (Админ-панель)
- **URL**: http://localhost:5173
- **Сборка для продакшена**: `pnpm run build` в папке frontend

### База данных (PostgreSQL)
- **Host**: localhost:5432
- **Database**: notification_db
- **Username/password**: согласно настройкам в docker-compose.yml

## 🔧 Настройка базы данных

### Через Docker (рекомендуется)
```bash
# Запуск базы данных и сервисов
pnpm run docker:up:detached

# Применение миграций Prisma
docker-compose -f docker/docker-compose.yml exec backend npm run prisma:deploy
```

### Локальная настройка (без Docker)
```bash
# Установите PostgreSQL локально и создайте базу данных

# Перейдите в папку бэкенда
cd apps/backend

# Установите зависимости
pnpm install

# Настройте .env файл с DATABASE_URL

# Примените миграции
pnpm run prisma:migrate

# Сгенерируйте Prisma клиент
pnpm run prisma:generate
```

## 📚 Документация компонентов

### Бэкенд (API сервер)
Полная документация доступна в [README бэкенда](apps/backend/README.md).

**Основные возможности:**
- Отправка push-уведомлений через Expo
- Регистрация устройств пользователей
- История отправленных уведомлений
- Аутентификация через Bearer токены
- Swagger документация API

### Фронтенд (Админ-панель)
Полная документация доступна в [README фронтенда](apps/frontend/README.md).

**Основные возможности:**
- Отправка push-уведомлений через веб-интерфейс
- Просмотр истории уведомлений
- Управление зарегистрированными устройствами
- Статистика отправки уведомлений
- Современный интерфейс на React + Tailwind CSS

## 🔐 Аутентификация

Система использует простую Bearer токен аутентификацию. В качестве токена используется `user_id`:

```http
Authorization: Bearer your_user_id_here
```

Пример:
```
Authorization: Bearer user123
```

Для админ-функций требуется токен пользователя с правами администратора.

## 🗄️ База данных

Проект использует **PostgreSQL** с **Prisma ORM**. Основные таблицы:

1. **devices** - зарегистрированные устройства пользователей
2. **notifications** - история отправленных уведомлений
3. **users** - информация о пользователях

Для управления базой данных используйте Prisma Studio:
```bash
# Через Docker
docker-compose -f docker/docker-compose.yml exec backend npm run prisma:studio

# Локально
cd apps/backend && npm run prisma:studio
```

## 🛠️ Разработка

### Добавление нового пакета
```bash
# В корневой директории
pnpm add <package-name> --filter <app-name>
# Пример: pnpm add axios --filter @monorepo/frontend
```

### Структура монорепозитория
- **apps/** - Приложения (бэкенд, фронтенд)
- **packages/** - Общие пакеты (shared)
- **docker/** - Конфигурация Docker
- **package.json** - Корневые зависимости и скрипты

### Общие зависимости
Общие зависимости для разработки находятся в корневом `package.json`:
- Turbo - для управления монорепозиторием
- TypeScript - для типизации
- Prettier - для форматирования кода
- Vite - для сборки фронтенда

## 🐛 Устранение неполадок

### Проблемы с зависимостями
```bash
# Очистка и переустановка
pnpm run clean
pnpm install

# Удаление node_modules и lock файлов
rm -rf node_modules pnpm-lock.yaml
rm -rf apps/*/node_modules packages/*/node_modules
pnpm install
```

### Проблемы с Docker
```bash
# Остановка всех контейнеров
docker-compose -f docker/docker-compose.yml down

# Удаление volumes (осторожно: удалит данные БД)
docker-compose -f docker/docker-compose.yml down -v

# Пересборка образов
pnpm run docker:build
pnpm run docker:up
```

### Проблемы с базой данных
```bash
# Сброс базы данных (только для разработки)
docker-compose -f docker/docker-compose.yml exec db psql -U postgres -c "DROP DATABASE IF EXISTS notification_db;"
docker-compose -f docker/docker-compose.yml exec db psql -U postgres -c "CREATE DATABASE notification_db;"

# Повторное применение миграций
docker-compose -f docker/docker-compose.yml exec backend npm run prisma:deploy
```

## 📄 Лицензия

[Укажите лицензию вашего проекта]

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для вашей фичи (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Отправьте в репозиторий (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📞 Поддержка

Для вопросов и поддержки:
1. Откройте Issue в репозитории
2. Проверьте документацию API: http://localhost:3000/api-docs
3. Изучите логи приложения через `pnpm run docker:logs`

---

**Примечание**: Этот проект является примером монорепозитория для системы push-уведомлений. Для продакшен использования рекомендуется:
1. Настроить правильные переменные окружения
2. Реализовать полноценную аутентификацию
3. Настроить мониторинг и логирование
4. Реализовать тестирование
5. Настроить CI/CD пайплайн